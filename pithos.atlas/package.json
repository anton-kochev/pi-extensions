{
  "name": "@pithos-kit/atlas",
  "version": "0.7.0",
  "description": "Session names, catalog, diagnostics, configuration, and guarded Pi runtime patches for pithos-kit.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "pithos",
    "atlas",
    "catalog",
    "configuration",
    "session-names",
    "session-naming",
    "runtime-patches",
    "footer"
  ],
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anton-kochev/pithos-kit.git",
    "directory": "pithos.atlas"
  },
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "pithos-atlas-patch": "./scripts/pi-footer-patch.mjs",
    "pithos-atlas-pi": "./scripts/pi-footer-launcher.mjs"
  },
  "scripts": {
    "catalog:generate": "node --import tsx ../scripts/generate-atlas-catalog.ts",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "diff": "^8.0.2",
    "semver": "^7.7.2",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.83.0",
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@earendil-works/pi-tui": "0.83.0",
    "@types/node": "^24.0.0",
    "@types/semver": "^7.7.0",
    "tsx": "^4.20.4",
    "typescript": "^5.9.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": ">=0.83.0",
    "@earendil-works/pi-coding-agent": ">=0.83.0",
    "@earendil-works/pi-tui": ">=0.83.0",
    "typebox": "*"
  },
  "pithosKit": {
    "displayName": "Atlas",
    "summary": "Name sessions, inspect and configure Pithos, and manage guarded Pi runtime patches.",
    "minimumPi": ">=0.83.0",
    "commands": [
      { "name": "pithos", "usage": "/pithos [help|packages|versions|doctor|config|patch]", "summary": "Open Atlas or run a catalog, diagnostic, configuration, or runtime-patch action." }
    ],
    "tools": [
      { "name": "rename_session", "summary": "Rename the current Pi session in lowercase kebab-case when explicitly requested." },
      { "name": "pithos_info", "summary": "Read pithos-kit catalog, version, runtime, and configuration information." }
    ],
    "prompts": [],
    "skills": [],
    "themes": [],
    "agents": [],
    "configuration": [
      { "kind": "file", "key": ".pithos", "summary": "Atlas manages toolchains, pi.version, and @pithos-kit entries under pi.extensions." },
      { "kind": "environment", "key": "PI_OFFLINE", "summary": "Disable registry and session-name model requests when true, yes, or 1." },
      { "kind": "environment", "key": "PITHOS_ATLAS_PI_PACKAGE_DIR", "summary": "Select the Pi package root for explicit standalone runtime-patch CLI operations." }
    ]
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "files": [
    "extensions",
    "scripts",
    "src",
    "README.md"
  ]
}
