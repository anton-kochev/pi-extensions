{
  "name": "@pithos-kit/plan",
  "version": "0.2.0",
  "description": "Enforced read-only planning with approved plan creation and contextual session naming for Pi.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "pi-prompt-template",
    "pithos",
    "plan-mode",
    "planning"
  ],
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.83.0",
    "@earendil-works/pi-tui": ">=0.83.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@earendil-works/pi-tui": "0.83.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anton-kochev/pithos-kit.git",
    "directory": "pithos.plan"
  },
  "publishConfig": {
    "access": "public"
  },
  "pithosKit": {
    "displayName": "Plan",
    "summary": "Provide enforced read-only planning, exact-draft approval, a Plan theme, and contextual session naming.",
    "minimumPi": ">=0.83.0",
    "commands": [
      { "name": "plan", "usage": "/plan [task | exit | cancel | --help]", "summary": "Enter, finalize, or explicitly exit an enforced read-only planning workflow." }
    ],
    "tools": [
      { "name": "create_plan", "summary": "Review and atomically create an approved plan during Plan mode.", "internal": true }
    ],
    "prompts": [
      { "name": "plan", "summary": "Enforced read-only planning workflow." }
    ],
    "skills": [],
    "themes": [
      { "name": "plan", "summary": "Minimal planning-state theme." }
    ],
    "agents": [],
    "configuration": [
      { "kind": "directory", "key": ".pi/plans", "summary": "Approved generated implementation plans." }
    ]
  },
  "pi": {
    "extensions": [
      "./extensions/plan-theme.ts"
    ],
    "prompts": [
      "./prompts"
    ],
    "themes": [
      "./themes"
    ]
  },
  "files": [
    "extensions",
    "prompts",
    "themes",
    "README.md"
  ]
}
